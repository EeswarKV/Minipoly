import React from 'react';

import {
    Text,Button
} from 'react-native';

export const WishScreen = ({ route, navigation }) => {
    debugger;
    // const { name } = route?.params?.name;
    // console.log("params", route.params.name);
        return (
            <>
                {/* <Text>Good Morning {route.params.name}</Text> */}
                <Text>Good Morning </Text>
            {/* <Button onPress={() => navigation.navigate('HomeScreen')} title="Go back home" /> */}
            </>
        );
    
};